apiVersion: v1
kind: Service
metadata:
  name: apartments-service
  namespace: apartments
spec:
  selector:
    app: app-java
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
  